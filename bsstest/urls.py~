from django.conf.urls import include, url

from bsstest import views,urls

urlpatterns = [
    
    url(r'^bsstest/$',views.bsstest),
    url(r'^register/$', views.register, name='register'), 
    
]						
